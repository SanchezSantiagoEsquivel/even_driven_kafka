apiVersion: apps/v1
kind: Deployment
metadata:
  name: lenin-kafka-producer
  labels:
    app: lenin-kafka-producer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lenin-kafka-producer
  template:
    metadata:
      labels:
        app: lenin-kafka-producer
    spec:
      containers:
        - name: lenin-kafka-producer
          image: leninecm/lenin-greetings
          env:
            - name: BOOTSTRAP_SERVERS
              value: "kafka-broker:9092"
            - name: TOPIC_NAME
              value: "univalle-ideas"
          ports:
            - containerPort: 9092
          command: ["python"]
          args: ["main.py"]
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      restartPolicy: Always
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: lenin-kafka-producer-config
  labels:
    app: lenin-kafka-producer
data:
  BOOTSTRAP_SERVERS: "kafka-broker:9092"
  TOPIC_NAME: "univalle-ideas"
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lenin-kafka-producer
  labels:
    app: lenin-kafka-producer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lenin-kafka-producer
  template:
    metadata:
      labels:
        app: lenin-kafka-producer
    spec:
      containers:
        - name: lenin-kafka-producer
          image: leninecm/lenin-greetings:latest
          env:
            - name: BOOTSTRAP_SERVERS
              value: "kafka-broker:9092"
            - name: TOPIC_NAME
              value: "univalle-ideas"
          ports:
            - containerPort: 9092
          command: ["python"]
          args: ["main.py"]
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      restartPolicy: Always
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: lenin-kafka-producer-config
  labels:
    app: lenin-kafka-producer
data:
  BOOTSTRAP_SERVERS: "kafka-broker:9092"
  TOPIC_NAME: "univalle-ideas"
